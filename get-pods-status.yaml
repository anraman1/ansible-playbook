---
- hosts: localhost
  tasks:
  - name: Login to ocp
    shell: oc login https://api-int.lab.ocp.lan:6443 -u ecslab -p "{{ ecslab_passwd }}" --kubeconfig=ecslab-kubeconfig --insecure-skip-tls-verify
  - name: Get pods 
    shell: oc get pods --kubeconfig=ecslab-kubeconfig
    register: pod_status
   
